import type {
  PostgresExerciseRow,
  PostgresWorkoutExerciseRow,
  PostgresWorkoutRow,
  PostgresWorkoutSetRow,
} from "@/types/workout";

export type {
  PostgresExerciseRow,
  PostgresWorkoutExerciseRow,
  PostgresWorkoutRow,
  PostgresWorkoutSetRow,
};
